node_modules/
dist/
build/
coverage/
# Tous les fichiers .env reels (audit securite OWASP #262 - .env, .env.local,
# .env.production...), sauf le gabarit versionne intentionnellement.
.env*
!.env.example
*.log
.DS_Store

# Données volumineuses du moteur de routage (export GTFS, extrait OSM, graphe compilé)
routing-engine/data/*
!routing-engine/data/.gitkeep

# Donnes superpowers (Claude Skill)
superpowers/

# Cache local du hook de detection Impeccable (chemins de fichiers absolus
# specifiques a la machine, pas d'interet en dehors de la session en cours)
.impeccable/hook.cache.json