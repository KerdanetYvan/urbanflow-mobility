# Plan de traitement — Sprint 5 (milestone Stretch)

> Ordre de traitement décidé en session le 2026-09-05, à la suite de la clôture du Sprint 4 (voir `sprint-4-retro.md`). Toutes les issues listées ici restent rattachées au milestone GitHub **"Stretch (post-MVP)"** — même décision qu'aux clôtures des Sprints 3 et 4 de ne pas créer de milestone "Sprint 5" séparé, ce fichier sert uniquement de séquencement de travail à l'intérieur de Stretch.

## Comment reprendre après une coupure de session

Les cases ci-dessous font foi pour savoir où on en est : `[ ]` pas commencé, `[*]` en cours (travail démarré, PR pas encore ouverte), `[x]` travail terminé et validé, PR ouverte. Reprendre au premier item non `[x]`. Si le dernier item `[x]` a encore sa PR ouverte (pas mergée), vérifier d'abord si elle est prête à merger avant de démarrer autre chose.

En cas de doute (case pas à jour, session interrompue en plein travail), vérifier le **Status** réel des issues sur le [GitHub Project](https://github.com/users/KerdanetYvan/projects/1) (`gh project item-list 1 --owner KerdanetYvan --format json`) et resynchroniser les cases ci-dessous en conséquence.

**Convention** (héritée de `sprint-2-plan.md`/`sprint-3-plan.md`/`sprint-4-plan.md`) :

- Cocher `[*]` dès qu'on commence à travailler sur une issue, avant le premier commit.
- Cocher `[x]` juste avant d'ouvrir la PR, une fois le travail terminé et validé (tests/lint/vérification manuelle passés) — toute la tâche part dans une seule PR, ouverte une fois le travail fini.
- **Ne pas ouvrir la PR sans le feu vert explicite de l'utilisateur**, même une fois le travail fini — signaler que la tâche est prête, attendre confirmation avant `gh pr create`.
- Le Status réel sur le board continue d'évoluer séparément via l'automatisation PR (`Closes #N`) et les workflows natifs du Project — la case `[x]` ici n'attend pas que la PR soit mergée, seulement que le travail soit fini et prêt à être proposé en revue.
- Au démarrage d'une phase, passer **toutes** ses issues en Status "In Progress" sur le board, pas seulement celle en cours de traitement.

## Ordre retenu

15 issues : 4 loose ends reportés du Sprint 4 ([#41](https://github.com/KerdanetYvan/urbanflow-mobility/issues/41)/[#42](https://github.com/KerdanetYvan/urbanflow-mobility/issues/42)/[#264](https://github.com/KerdanetYvan/urbanflow-mobility/issues/264)/[#268](https://github.com/KerdanetYvan/urbanflow-mobility/issues/268)) + 11 constats de la revue testeur qui a clôturé le Sprint 4 ([#272](https://github.com/KerdanetYvan/urbanflow-mobility/issues/272)–[#282](https://github.com/KerdanetYvan/urbanflow-mobility/issues/282)).

**Principe de priorisation retenu (décision utilisateur en session, remplace le regroupement par fonctionnalité initialement proposé)** : on ne trie plus par brique technique mais par **visibilité de l'issue** — une carte mal centrée ou des glyphes trop petits touchent quiconque lance une recherche, connecté ou non, alors qu'une 500 sur `PATCH /profiles/me` ne touche qu'un utilisateur connecté en train d'éditer son profil, un cas plus proche du "bug MVP" ponctuel. D'où 3 paliers, dans cet ordre :

1. **Visibilité générale** (`/recherche`, connecté ou non) — la première chose vue par n'importe quel visiteur.
2. **Visibilité connecté** (profil, historique, suivi de trajet) — visible seulement après connexion.
3. **Fonctionnel / dette** — bugs et travaux sans visibilité UI directe (sécurité, tests, process).

### Phase A — Visibilité générale (`/recherche`, connecté ou non)

- [x] [#272](https://github.com/KerdanetYvan/urbanflow-mobility/issues/272) (Dev FE) — **Priorité du sprint** (décision utilisateur) : remplacer les 2 tailles de glyphes (normale/grande) par 4 (petite/moyenne/grande/très grande) — `useGlyphScale.ts`, `useGlyphSizePreference.ts`, sélecteur dans `ProfilPage.tsx`. Affiché sur la carte dès la première recherche, avant même toute connexion. Grillé en session (`/grill-me`) : "moyenne" devient le nouveau défaut (l'ancien maximum restait perçu comme minuscule), facteur d'écran de base revu à la hausse. Périmètre resserré après vérification visuelle réelle (Playwright) : seuls les pins origine/destination suivent ce réglage, pas les stations GBFS/correspondances/position (chevauchement illisible constaté en zone dense mobile sinon). Tests/lint/build verts, en attente de vérification locale par l'utilisateur avant PR.
- [x] [#273](https://github.com/KerdanetYvan/urbanflow-mobility/issues/273) (Dev FE) — Corriger le padding de `fitBounds` (`MapView.tsx`) pour qu'il tienne compte des panneaux flottants qui chevauchent la carte, par breakpoint — même écran que #272. Grillé en session (`/grill-me`) : mesure réelle des panneaux (`ResizeObserver`) plutôt qu'une constante figée, recalcul sur repli/déploiement du bandeau mobile en plus du changement d'itinéraire. Nouveau hook `useMapSafeAreaPadding` ; cas "point unique/vue par défaut" traité par projection du centre plutôt que `panBy`. Vérifié visuellement en réel (Playwright) : décalage horizontal correct en vue par défaut desktop, recadrage effectif au repli du bandeau mobile. Tests/lint/build verts.
- [x] [#274](https://github.com/KerdanetYvan/urbanflow-mobility/issues/274) (Dev FE) — Ne plus afficher le badge "Le plus adapté à vos critères" quand aucun critère n'est sélectionné (`itineraryBadges.ts`, `computeItineraryBadges`) — remplacer par un libellé neutre ("Trajet le plus rapide"). Visible sur quasi toutes les recherches sans profil. Grillé en session (choix multiple) : vérifié côté scoring backend que la durée domine largement par défaut, "trajet le plus rapide" reste donc fidèle ; n'importe quelle préférence cochée (y compris `wheelchair_accessible` seul) garde l'ancien libellé, décision utilisateur pour la simplicité. Tests/lint/build verts.
- [x] [#279](https://github.com/KerdanetYvan/urbanflow-mobility/issues/279) (Dev FE) — Le texte "Voir le détail" s'affichait en fait h24 (desktop ET mobile), régression par rapport à #170 (règle CSS de masquage desktop disparue, probablement lors des refontes #233/#234). Retrait complet plutôt que remasquage : jamais un lien/bouton distinct, la carte entière affiche déjà le détail au tap/clic. Vérifié visuellement en réel (Playwright) desktop et mobile.
- [x] [#280](https://github.com/KerdanetYvan/urbanflow-mobility/issues/280) (Dev FE) — Revu en session après un premier essai jugé insuffisant : le détail ne doit pas juste apparaître inline sous la liste (fallait scroller), mais **par-dessus** la carte recherche+liste en mobile, qui se masque alors complètement ("libérer l'espace"). Nouvelle carte `.resultats-mobile-detail-overlay` avec bouton "Retour à la liste" explicite ; flow strict à 2 états (liste OU détail, jamais les deux — il faut fermer le détail pour changer de sélection). Sans effet en desktop (panneau latéral #234 inchangé). Vérifié visuellement en réel (Playwright) : cycle complet liste → tap → overlay → fermeture → liste.
- [x] [#275](https://github.com/KerdanetYvan/urbanflow-mobility/issues/275) (Dev BE/FE) — Remonté le détail réel d'une perturbation (`kind`/`headerText` de `RealtimeDisruption`) jusqu'à `TripItinerary` (nouveau `disruptionDetails`, DTO `TripDisruptionDetail`), affiche un message spécifique par nature (annulation/arrêt sauté/texte d'alerte opérateur) à la place de l'encadré générique — tiret cadratique reformulé au passage. `ScoringService#hasActiveDisruption` devient `#findItineraryDisruptions`, renvoie les perturbations trouvées au lieu d'un simple booléen. +3 tests backend, +5 frontend.

### Phase B — Visibilité connecté (profil, historique, suivi de trajet)

- [x] [#278](https://github.com/KerdanetYvan/urbanflow-mobility/issues/278) (Dev FE/BE) — Retirer covoiturage/trottinette du sélecteur de préférences de profil. `TransportMode` backend laissé intact (partagé avec recherche/suivi/OTP/seed — le vider casserait ces surfaces pour rien) ; nouveau sous-ensemble `PROFILE_TRANSPORT_MODES` que seuls les DTO de profil valident, `POST/PATCH /profiles` rejette désormais ces 2 valeurs. Lecture tolérante : colonne `text[]` (aucune contrainte SQL) + filtre au chargement côté front. `TRANSPORT_MODES` front est partagé profil ↔ filtre `/recherche` : les 2 écrans perdent les 2 options (cohérent avec le constat testeur). Groupe "Partagé" du profil supprimé (devenu vide). +5 tests backend (validation DTO), +1 frontend (lecture tolérante d'un profil legacy). Tests/lint/build verts.
- [x] [#281](https://github.com/KerdanetYvan/urbanflow-mobility/issues/281) (Dev FE/BE) — Historique cassé en prod. **Cause racine confirmée par diagnostic SSH prod** : `GEOLOCATION_ENCRYPTION_KEY` **totalement absente** du `.env` VPS (même classe que #238, `.env` prod maintenu à la main). Conséquence : `TripHistoryService#record` lève à chaque écriture chiffrée mais avale l'erreur par conception → aucune ligne jamais écrite (`trip_history_entries` : 0 ligne en prod, migration pourtant jouée), `GET /trips/history` renvoie `200 []`. Hypothèse "migration non jouée" éliminée. **Volet A (ops, fait)** : clé générée + ajoutée au `.env` prod + conteneur `backend` recréé (les 4 tables chiffrées étaient vides, aucune donnée à re-chiffrer). **Volet B (cette PR)** : (1) `validate` dans `ConfigModule.forRoot` (`src/config/env.validation.ts`) — fail-fast au boot si `GEOLOCATION_ENCRYPTION_KEY`/`DATABASE_URL`/`JWT_SECRET`/`JWT_REFRESH_SECRET` absent ou clé ≠ 32 octets → un secret critique manquant redevient un échec de déploiement visible, pas une perte de données silencieuse ; (2) `findRecent` résilient — repli SQL brut + déchiffrement ligne à ligne, une entrée indéchiffrable (clé rotée, ligne corrompue) est ignorée+logguée au lieu de faire tomber toute la réponse. `decodeEncryptionKey` extrait du transformer et partagé. +10 tests backend. Même clé manquante cassait aussi `mobility_profiles` home/work (→ #282) et `push_subscriptions` (→ #277) : le volet A les répare aussi, à re-tester.
- [x] [#276](https://github.com/KerdanetYvan/urbanflow-mobility/issues/276) (PO) — Décision PO en session : **la contrainte reste** (suivi réservé aux comptes). Suivi anonyme local (s'arrête à la fermeture de l'app → demi-fonctionnalité trompeuse) et suivi anonyme complet (jeton d'appareil → nouvelle surface d'écriture publique à cadrer, coût sans rapport avec le gain post-MVP) pesés et écartés. Figé dans `docs/specs/f3-scoring-perturbations-suivi.md` §3.1 pour ne plus le rouvrir sans élément nouveau. Doc seule, aucun code.
- [x] [#277](https://github.com/KerdanetYvan/urbanflow-mobility/issues/277) (Dev FE) — `subscribeBrowserToPush` renvoie désormais un `PushSubscribeOutcome` discriminé au lieu de `PushSubscriptionPayload | null` : `unsupported` / `server-unconfigured` / `permission-blocked` (refus déjà mémorisé → oriente vers les réglages du navigateur, aucune redemande possible) / `permission-denied` (refus à l'instant) / `permission-dismissed` (popup fermé sans choix) / `error` / `subscribed`. `TripFollowButton` mappe chaque cause à un titre + message de repli propre (fini le « Notifications désactivées » unique). +8 tests `push.spec.ts`, +3 `TripFollowButton.spec.tsx`. **Vérif VAPID prod (faite)** : `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT` étaient absentes du `.env` VPS (même piège que `GEOLOCATION_ENCRYPTION_KEY` #281) → ajoutées côté ops, endpoint vérifié ; 2 lignes cruft retirées (`NOMATIM_URL` typo, `WEATHER_API_KEY` inutilisé). Prévention → issue #293. **Note (diag #281)** : `push_subscriptions` chiffre `endpoint`/`p256dh`/`auth` avec `GEOLOCATION_ENCRYPTION_KEY`, absente du `.env` prod → l'enregistrement d'abonnement échouait aussi. Corrigé côté ops par le volet A de #281.

### Phase C — Fonctionnel / dette (sans visibilité UI directe)

- [x] [#282](https://github.com/KerdanetYvan/urbanflow-mobility/issues/282) (Dev BE) — **Même cause racine que #281**, résolu sans code. Le seul chemin vers un 500 sur `PATCH /profiles/me` = `save()` qui lève sur la colonne chiffrée `home*/work*` faute de `GEOLOCATION_ENCRYPTION_KEY` (les enums invalides donnent un 400, jamais un 500). Clé ajoutée au `.env` prod (volet A de #281) + `validateEnv` (PR #290) empêche la récidive silencieuse. **Vérifié en prod** : sauvegarde d'une adresse domicile/travail → 200, plus d'erreur. Issue fermée avec compte-rendu.
- [x] [#268](https://github.com/KerdanetYvan/urbanflow-mobility/issues/268) (Dev BE) — Rotation stricte du refresh token (OWASP A07, Annexe C). Nouvelle table `refresh_tokens` (`jti` + `family_id` = session, `consumed_at`/`revoked_at`/`replaced_by`) + migration. `AuthService.refresh()` : usage unique (jeton présenté marqué consommé et remplacé) + détection de rejeu (jeton déjà consommé re-présenté hors fenêtre de grâce 15 s → révocation de **toute la famille**, déconnexion forcée). Fenêtre de grâce + single-flight client (`frontend/src/lib/api.ts`) pour les rafraîchissements concurrents multi-onglets. Purge `@Cron` quotidienne des lignes expirées. Jetons legacy (sans `jti`) refusés. +9 tests BE (`auth.service.spec.ts`), +2 FE (`api.spec.ts`). `docs/audits/owasp-audit.md` §4 : limite levée. Hors périmètre : `POST /auth/logout` serveur (suivi possible).
- [x] [#264](https://github.com/KerdanetYvan/urbanflow-mobility/issues/264) (Dev FE) — Test e2e WCAG `frontend/e2e/wcag-audit.spec.ts` réécrit et réactivé (`.fixme` retiré). L'UI ciblée avait évolué au-delà du constat de l'issue : l'ancien popover dédié (#108/#109) → chips (#255) → **fusionné dans la modale unique « Filtres » (#233)**. Le test vérifie désormais le contrat clavier réel de cette modale : ouverture au clavier (Entrée sur le déclencheur), focus déplacé dans la modale à l'ouverture, piège de focus (Tab/Shift+Tab aux bornes), activation d'une case à l'Espace, fermeture à Échap avec retour du focus au déclencheur. Dette de test pure, aucune visibilité produit.
- [ ] [#293](https://github.com/KerdanetYvan/urbanflow-mobility/issues/293) (Dev BE/Ops) — Étape CI de détection de dérive du `.env` prod (diff des **clés** présentes sur le VPS vs `.env.example`, allowlist versionnée des absences légitimes). Issue née des diagnostics #281/#277 : 3 dérives silencieuses coup sur coup (`GEOLOCATION_ENCRYPTION_KEY`, les 3 `VAPID_*`, + cruft `NOMATIM_URL`/`WEATHER_API_KEY`).
- [ ] [#42](https://github.com/KerdanetYvan/urbanflow-mobility/issues/42) (PO) — Documenter en continu les choix d'architecture
- [ ] [#41](https://github.com/KerdanetYvan/urbanflow-mobility/issues/41) (PO) — Préparer le support de démonstration pour la soutenance

## Pourquoi cet ordre (raisonnement complet en cas de doute)

- **Priorisation par visibilité plutôt que par brique technique** (décision utilisateur en session, remplace le découpage par fonctionnalité de la première version de ce plan) : ce qui est vu par le plus grand nombre d'utilisateurs, connectés ou non, passe avant ce qui n'est vu que par un utilisateur connecté, qui passe lui-même avant ce qui n'a aucune visibilité produit directe (sécurité, tests, process). Un bug "fonctionnel" comme [#282](https://github.com/KerdanetYvan/urbanflow-mobility/issues/282) (500 ponctuelle sur le profil) compte comme un simple bug MVP, pas comme prioritaire face à un défaut visible sur l'écran d'entrée de l'app.
- **[#272](https://github.com/KerdanetYvan/urbanflow-mobility/issues/272) en tête absolue** : les glyphes de carte s'affichent dès la première recherche, avant toute connexion — la surface la plus visible du produit.
- **Phase A ordonnée par fréquence d'affichage réelle** : carte ([#272](https://github.com/KerdanetYvan/urbanflow-mobility/issues/272)/[#273](https://github.com/KerdanetYvan/urbanflow-mobility/issues/273), toujours visible) puis liste de résultats ([#274](https://github.com/KerdanetYvan/urbanflow-mobility/issues/274)/[#279](https://github.com/KerdanetYvan/urbanflow-mobility/issues/279)/[#280](https://github.com/KerdanetYvan/urbanflow-mobility/issues/280), quasi toujours visible) puis détail conditionnel ([#275](https://github.com/KerdanetYvan/urbanflow-mobility/issues/275), seulement si une perturbation existe réellement). [#280](https://github.com/KerdanetYvan/urbanflow-mobility/issues/280) juste après [#279](https://github.com/KerdanetYvan/urbanflow-mobility/issues/279) : même nettoyage desktop/mobile, à ne pas séparer.
- **Phase B, [#276](https://github.com/KerdanetYvan/urbanflow-mobility/issues/276) avant [#277](https://github.com/KerdanetYvan/urbanflow-mobility/issues/277)** : la décision PO sur l'obligation de compte peut faire évoluer le flux de permission notification, pas de raison technique de les inverser.
- **Phase C, [#282](https://github.com/KerdanetYvan/urbanflow-mobility/issues/282) en tête** : reste un bug utilisateur (même ponctuel), à traiter avant la dette pure (sécurité/tests/process) qui n'a aucune visibilité produit. [#42](https://github.com/KerdanetYvan/urbanflow-mobility/issues/42)/[#41](https://github.com/KerdanetYvan/urbanflow-mobility/issues/41) restent en tout dernier, même raisonnement qu'aux Sprints 3 et 4 (démo/doc n'ont de sens qu'une fois le reste stabilisé).

**Point de vigilance** : contrairement au Sprint 4 (28 issues, delta connu dès le départ), ce plan ne compte que 15 issues séquencées — mais l'expérience du Sprint 4 montre qu'une bonne part du travail réel se découvre en testant en conditions réelles plutôt qu'en planifiant à l'avance. Ne pas être surpris si de nouvelles issues s'ajoutent en cours de route.
